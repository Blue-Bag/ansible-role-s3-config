---
# defaults file for s3-config

s3_createdefaultbucket: true
s3cmdcfg_bucket: "{{ inventory_hostname }}"
s3cmdcfg_bucket_location: 'EU'

aws_access_key: redacted
aws_secret_key: redacted

s3cfg_pkgs:
  - python-setuptools
  - python-dateutil
  - python-magic
  - python-boto3
  - python-botocore
